---
- name: Do gentoo world rebuild
  hosts: laptop
  become: yes
  tasks:
  - name: Sync portage/layman trees and index ebuilds
    eix:
      action: sync
  - name: Rebuild the @world
    notify: ensure-services-started
    portage:
      name: "@world"
      state: emerged
      deep: yes
      update: yes
      verbose: yes
      newuse: yes
      loadavg: 4
      jobs: 9
  - name: Rebuild the @preserved-rebuild
    notify: ensure-services-started
    portage:
      name: "@preserved-rebuild"
      state: emerged
      verbose: yes
      loadavg: 4
      jobs: 9
